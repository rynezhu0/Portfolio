// The root route is the resting tile-row browsing state — the tile row
// itself lives in the persistent <Scene> canvas (see AppShell in the root
// layout), so there's no route-specific content to render here.
export default function HomePage() {
  return null;
}
